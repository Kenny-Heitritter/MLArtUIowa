List of Things to Do:
-Figure out why I can't get a better accuracy using Pytorch architecture than my initial diy multi-layer perceptron. They're fairly close (~21% vs ~24% accuracy) but I would think introducing convolutional neural network layers would improve training.

